// basic syntax

let str = `Template literal in ES6`

console.log(str);
console.log(str.length);
console.log(typeof str);

let anotherStr = `Here is another template literal`

let strWithBacktick = `Template literals use backticks \ instead`

let msg = ['This text', 'can', 'span multiple lines'].join('\n');

